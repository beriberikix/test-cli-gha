name: Create release with CLI

on:
  workflow_dispatch:
  
jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - run: gh release list
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
